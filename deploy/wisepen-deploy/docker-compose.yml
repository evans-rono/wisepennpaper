services:
  app:
    build: .
    ports: ["3000:3000"]
    env_file: .env
    environment:
      DB_PATH: /app/data/wise-pen.db
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
